PRIVATE_KEY=your_keeper_wallet_private_key_here
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override cron schedule for testing (default: daily at midnight)
# AGENT_CRON=*/1 * * * *   <- runs every minute (dev/demo mode)
